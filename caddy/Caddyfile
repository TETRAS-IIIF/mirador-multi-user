:80 {
	root * /srv
    file_server {
        browse off
    }
	header Access-Control-Allow-Origin "{env.CORS_ALLOWED_HOSTS}"

	 handle /favicon.ico {
        root * /srv/public/
        file_server
    }
}
